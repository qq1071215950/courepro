server:
  port: 9001
spring:
  application:
    name: business
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/course?characterEncoding=utf-8&amp&serverTimezone=UTC&amp&useSSL=false
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

