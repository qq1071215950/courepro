spring:
  datasource:
    url: jdbc:mysql://localhost:3306/course?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: 127.0.0.1
    port: 6379
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
logging:
  level:
    com:
      course:
        server:
          mapper=trace:
